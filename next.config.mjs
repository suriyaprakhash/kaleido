/** @type {import('next').NextConfig} */
const nextConfig = {
    // dev: false,
    // distDir: 'dist',
    output: 'export',
    images: {unoptimized: true},
    // poweredByHeader: false
};

export default nextConfig;
